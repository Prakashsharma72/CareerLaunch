# ── Local development ──────────────────────────────────────────────────────
# Vite exposes variables prefixed with VITE_ to the browser bundle.
# This file is NOT committed with real secrets — safe for local URLs.

VITE_API_URL=http://localhost:5000/api
