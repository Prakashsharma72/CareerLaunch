# ── Production (Vercel) ────────────────────────────────────────────────────
# This value is also set in the Vercel dashboard under
# Project → Settings → Environment Variables so it is available
# at build time on Vercel even if this file is gitignored.
#
# Replace the URL below with your actual Render service URL.

VITE_API_URL=https://careerlaunch-api.onrender.com/api
